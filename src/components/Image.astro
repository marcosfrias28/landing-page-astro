---
interface Props {
  src: string;
  alt: string;
  loading?: "eager" | "lazy";
  className?: string;
}
const { src, alt, loading, className } = Astro.props;
---

<img class={className} src={src} alt={alt} loading={loading} {...Astro.props} />
