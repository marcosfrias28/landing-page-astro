---
interface Props {
  src: string;
  alt: string;
  loading?: "eager" | "lazy";
  class?: string;
}
const { src, alt, loading } = Astro.props;
---

<img
  class={Astro.props.class}
  src={src}
  alt={alt}
  loading={loading}
  {...Astro.props}
/>
