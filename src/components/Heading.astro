---
import Image from "../components/Image.astro";

interface Props {
  logo?: string;
  alt?: string;
  loading?: "eager" | "lazy";
  title: string;
  description: string;
  titleSize: string;
}

const { logo, alt, loading, title, description, titleSize } = Astro.props;
---

<text class="">
  {
    logo && (
      <Image loading={loading} class="mx-auto my-8" src={logo} alt={alt} />
    )
  }
  <h2
    style=`font-size: ${titleSize};`
    class=`font-medium text-nandor-700 text-wrap mb-4`
  >
    {title}
  </h2>
  <p class="text-pretty text-aluminium-500 text-[16px]">
    {description}
  </p>
</text>
