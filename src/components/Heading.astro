---
import Image from "../components/Image.astro";

interface Props {
  logo?: string;
  alt?: string;
  loading?: "eager" | "lazy";
  title: string;
  description: string;
  titleSize: string;
}

const { logo, alt, loading, title, description, titleSize } = Astro.props;
---

<text>
  {
    logo && (
      <Image loading={loading} className="mx-auto my-5" src={logo} alt={alt} />
    )
  }
  <h2
    style=`font-size: ${titleSize};`
    class=`font-medium text-nandor-900 text-balance mb-4`
  >
    {title}
  </h2>
  <p class="text-pretty text-aluminium-500 text-[18px]">
    {description}
  </p>
</text>
